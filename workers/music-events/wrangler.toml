name = "music-events"
main = "src/index.js"
compatibility_date = "2024-04-01"

# Create a KV namespace in the dashboard (Storage & Databases → KV),
# then paste its id here and run: wrangler deploy
[[kv_namespaces]]
binding = "MUSIC_LOGS"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
